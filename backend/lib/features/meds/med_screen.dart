import 'package:flutter/material.dart';

class MedScreen extends StatelessWidget {
  const MedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Meds Page')),
    );
  }
}
